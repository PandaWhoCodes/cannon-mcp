# Service: Backend

## Getting Context

Use the `grounding-docs` MCP server for cross-service context:

- `get_architecture` — system-wide architecture
- `get_api_contracts` — how services communicate
- `get_doc("backend", "CLAUDE.md")` — this service's detailed context
- `get_doc("frontend", "CLAUDE.md")` — frontend context (when working on APIs)
- `search_docs("keyword")` — find any doc mentioning a topic

## Local Context

<!-- Add backend-specific instructions, patterns, gotchas here -->
